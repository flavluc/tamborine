# MongoDB connection string for the API.
# Includes protocol, host, port, and database name.
# Must remain private. Never expose to frontend.
DATABASE_URL=mongodb://mongodb:27017/tamborine

# Secret key for signing ACCESS JWTs.
# Used to generate and verify short-lived authentication tokens.
# Must be random, high-entropy (≥32 bytes), and kept secret.
JWT_ACCESS_SECRET=supersecureaccesskey1234567890

# Public URL of the web (frontend) application.
# Used by the API for CORS, redirects, and absolute links.
# Safe to expose.
WEB_URL=http://localhost:3000
